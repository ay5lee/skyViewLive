server {
    listen 80;

    location /api/ {
        proxy_pass http://proxy:8765/;
        proxy_set_header Host $host;
        proxy_read_timeout 30s;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
